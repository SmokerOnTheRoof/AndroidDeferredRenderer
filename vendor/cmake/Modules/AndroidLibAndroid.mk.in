LOCAL_PATH := $(call my-dir) 

include $(CLEAR_VARS) 

LOCAL_MODULE := @libname@

LOCAL_SRC_FILES := 	lib@libname@.so 

LOCAL_SHARED_LIBRARIES := @SE_ANDROID_STL_LIBNAME@

include $(PREBUILT_SHARED_LIBRARY)